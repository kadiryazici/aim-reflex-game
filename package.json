{
   "name": "vierone-youtube-ders",
   "version": "0.0.0",
   "scripts": {
      "dev": "vite",
      "build": "vite build",
      "deploy": "npm run build && firebase deploy",
      "b:d:p": "yarn build && firebase deploy && git add . && git commit -m \"difficulty increased\" && git push origin main"
   },
   "dependencies": {
      "typescript": "^4.1.3",
      "vue": "3.0.4",
      "vue-router": "next",
      "vuex": "^4.0.0-rc.2"
   },
   "devDependencies": {
      "@vue/compiler-sfc": "^3.0.4",
      "sass": "latest",
      "vite": "latest"
   }
}
